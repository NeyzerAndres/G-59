# from . import views
# from django.urls import path
# from django.urls import path
# from .views import PasswordResetRequestView, PasswordResetConfirmViewCustom, PasswordResetCompleteViewCustom


# # urlpatterns = [
# #     # Ruta para la solicitud de restablecimiento de contraseña
# #     path('reset_password/', PasswordResetRequestView.as_view(), name='reset_password'),
    
# #     # Ruta para la confirmación de restablecimiento de contraseña
# #     path('reset/<uidb64>/<token>/', PasswordResetConfirmViewCustom.as_view(), name='password_reset_confirm'),
    
# #     # Ruta para la confirmación de restablecimiento de contraseña completado
# #     path('reset/done/', PasswordResetCompleteViewCustom.as_view(), name='password_reset_complete'),

# #     # path('pagina_principal/', views.Pagina_principal.as_view()),
# #     # path('busqueda_productos/', views.busqueda_productos),
# #             ]